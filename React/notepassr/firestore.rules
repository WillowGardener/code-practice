rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth.token.email.matches('.*@ddsd40[.]org') || request.auth.token.email.matches('.*@stu.ddsd40[.]org');
    }
  }
}